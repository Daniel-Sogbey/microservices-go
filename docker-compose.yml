version: "3"

services:
  broker-service:
    build:
      context: ./broker-service
      dockerfile: broker-service.dockerfile
    restart: always
    ports:
      - "8080:80" #8080 local 80 in docker image
    deploy:
      mode: replicated
      replicas: 1